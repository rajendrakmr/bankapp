apiVersion: v1 
kind: Service 
metadata: 
  name: mysql-service 
  namespace: bankapp-ns 
  labels: 
    app: mysql-service 

spec:
  type: NodePort
  selector:
    app: mysql-service
  ports:
  - protocol: TCP
    port: 3306
    targetPort: 3306
