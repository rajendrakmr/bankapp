kind: Service
apiVersion: v1 
metadata:
  name: mysql-service 
  namespace: bank-ns 
  labels:
    app: mysql-service 
spec:
  selector:
    app: bankapp-deploy
  type: NodePort
  ports:
    - protocol: TCP 
      targetPort: 8080
      port: 8080  
